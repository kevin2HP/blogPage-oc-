<%- include('partials/header'); -%>

<h1>Home</h1>
    <% posts.forEach(function(post){ %>
        

            <h1><%=post.Title%></h1> 
            <p><%=post.content.substring(0,50)+"..."%> <a href="/posts/<%= post.Title %> ">Read More</a></p>

            
        <% console.log(post.Title);%>

 <%})%>
<%- include('partials/footer'); -%>
